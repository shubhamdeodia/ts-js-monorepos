{
  "name": "@shlack/types",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist *.tsbuildinfor",
    "build": "tsc -b .",
    "lint": "eslint src --ext js,ts",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.12.16",
    "@babel/preset-typescript": "^7.12.16",
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "typescript": "^4.0.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "moduleNameMapper": {
      "@shlack/types": "./src",
      "@shlack/utils": "../utils/src"
    }
  },
  "dependencies": {
    "@types/date-fns": "^2.6.0",
    "@types/react": "^17.0.2",
    "date-fns": "^2.17.0",
    "react": "^17.0.1"
  }
}
